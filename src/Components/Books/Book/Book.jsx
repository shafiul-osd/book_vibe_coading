import { Link } from "react-router";


const Book = ({book}) => {

    const { bookName,image,author,review} = book

    return (
        <div>
         <Link>
         
         
         </Link>
        </div>
    );
};

export default Book;